# git-status-subdirectories

```bash
npm start
```

<img src="docs/assets/console.png" width="432">

**RU:** Выполняет команду `git status -s` для всех смежных проектов.

**EN:** Runs the `git status -s` command for all adjacent projects.
